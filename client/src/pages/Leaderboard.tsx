import {useEffect,useState} from 'react';import {Trophy} from 'lucide-react';import Layout from '../components/Layout';import {Card} from '../components/UI';import {api} from '../lib/api';export default function Leaderboard(){const[list,setList]=useState<any[]>([]);useEffect(()=>{api.get('/leaderboard').then(r=>setList(r.data.leaderboard))},[]);return <Layout><div className="flex items-end justify-between"><div><h1 className="font-display text-3xl">Leaderboard</h1><p className="mt-2 text-slate-500">Legends inspire legends. Climb together.</p></div><Trophy className="text-amber-300" size={30}/></div><Card className="mt-6 overflow-hidden"><div className="grid grid-cols-[45px_1fr_70px_90px] gap-3 border-b border-white/5 p-4 text-xs uppercase tracking-wider text-slate-500"><span>#</span><span>Hero</span><span>Level</span><span>Gold</span></div>{list.map(u=><div key={u.id} className="grid grid-cols-[45px_1fr_70px_90px] items-center gap-3 border-b border-white/5 p-4"><span className={u.rank<=3?'text-amber-300':'text-slate-500'}>{u.rank}</span><span className="font-medium">{u.name}</span><span>Lv. {u.level}</span><span className="gold">{u.gold}</span></div>)}</Card></Layout>}
