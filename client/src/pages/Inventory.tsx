import {useEffect,useState} from 'react';import Layout from '../components/Layout';import {Card,Empty} from '../components/UI';import {api} from '../lib/api';export default function Inventory(){const[items,setItems]=useState<any[]>([]);useEffect(()=>{api.get('/inventory').then(r=>setItems(r.data.items))},[]);return <Layout><h1 className="font-display text-3xl">Inventory</h1><p className="mt-2 text-slate-500">Items and rewards collected on your journey.</p><Card className="mt-6 p-5"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{items.map(i=><div key={i.id} className="rounded-2xl border border-white/5 bg-white/[.025] p-5 text-center"><div className="mx-auto grid h-20 w-20 place-items-center rounded-2xl bg-gradient-to-br from-amber-400/20 to-purple-500/10 text-4xl">{i.item_type==='badge'?'🏆':'🌌'}</div><h3 className="mt-4 font-display">{i.name}</h3><p className="mt-1 text-xs text-slate-500">{i.description}</p><span className="mt-3 inline-block text-xs gold">{i.rarity}</span></div>)}{!items.length&&<Empty text="Your inventory is waiting for its first treasure."/>}</div></Card></Layout>}
