import {FormEvent,useState} from 'react';import {Link,useNavigate,useLocation} from 'react-router-dom';import {Shield} from 'lucide-react';import {api} from '../lib/api';import {useAuth} from '../hooks/useAuth';
export default function Auth({mode}:{mode:'login'|'register'}){const[form,setForm]=useState({name:'',email:'',password:''}),[error,setError]=useState('');const nav=useNavigate();const{setUser}=useAuth();const submit=async(e:FormEvent)=>{e.preventDefault();setError('');try{const r=await api.post(`/auth/${mode}`,form);localStorage.setItem('ascend_token',r.data.token);setUser(r.data.user);nav('/dashboard')}catch(e:any){setError(e.response?.data?.message||'Something went wrong')}};return <div className="grid min-h-screen place-items-center p-5"><div className="w-full max-w-md"><div className="mb-8 text-center"><Shield className="mx-auto gold" size={34}/><div className="mt-2 font-display text-3xl">Ascend</div><p className="mt-2 text-sm text-slate-500">{mode==='login'?'Welcome back, hero.':'Create your hero and begin the journey.'}</p></div><form onSubmit={submit} className="glass rounded-3xl p-6 sm:p-8">{mode==='register'&&<label className="mb-4 block text-sm">Name<input required className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 outline-none focus:border-amber-400" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label>}<label className="mb-4 block text-sm">Email<input required type="email" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 outline-none focus:border-amber-400" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="mb-4 block text-sm">Password<input required minLength={6} type="password" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 outline-none focus:border-amber-400" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{error&&<div className="mb-4 rounded-xl bg-rose-500/10 p-3 text-sm text-rose-300">{error}</div>}<button className="btn-gold w-full rounded-xl p-3">{mode==='login'?'Enter the Realm':'Create Hero'}</button><p className="mt-5 text-center text-sm text-slate-500">{mode==='login'?<>New hero? <Link className="gold" to="/register">Register</Link></>:<>Already have an account? <Link className="gold" to="/login">Login</Link></>}</p></form><Link to="/" className="mt-5 block text-center text-sm text-slate-600">← Back to home</Link></div></div>}
