import {useEffect,useState} from 'react';
import {Award,Lock,CheckCircle2,Swords,Flame,Coins,BookOpen,Star} from 'lucide-react';
import Layout from '../components/Layout';import {Card,Gold} from '../components/UI';import {api} from '../lib/api';
const icons:any={quests:Swords,streak:Flame,gold:Coins,level:Star,inventory:Award,learning:BookOpen};
export default function Achievements(){const[data,setData]=useState<any[]>([]);useEffect(()=>{api.get('/achievements').then(r=>setData(r.data.achievements)).catch(()=>{})},[]);return <Layout><div><h1 className="font-display text-3xl">Achievements</h1><p className="mt-2 text-slate-500">Milestones that prove your progress.</p></div><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{data.map(a=>{const I=icons[a.icon]||Award;return <Card key={a.key} className={`p-5 ${a.unlocked?'border-amber-400/30':'opacity-75'}`}><div className="flex items-start justify-between"><div className={`grid h-12 w-12 place-items-center rounded-xl ${a.unlocked?'bg-amber-400/10 gold':'bg-white/5 text-slate-500'}`}><I/></div>{a.unlocked?<CheckCircle2 className="text-emerald-400"/>:<Lock className="text-slate-600" size={18}/>}</div><h3 className="mt-4 font-display text-lg">{a.name}</h3><p className="mt-1 text-sm text-slate-500">{a.description}</p><div className="mt-4 flex items-center justify-between text-xs"><span>{a.progress} / {a.target}</span><span className={a.unlocked?'text-emerald-400':'text-slate-500'}>{a.unlocked?'Unlocked':'In progress'}</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-900"><div className="h-full rounded-full bg-gradient-to-r from-amber-400 to-cyan-400" style={{width:`${Math.min(100,a.progress/a.target*100)}%`}}/></div></Card>})}</div></Layout>}
