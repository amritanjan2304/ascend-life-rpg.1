import {ReactNode,useState} from 'react';import {NavLink,useNavigate} from 'react-router-dom';import {LayoutDashboard,Swords,Backpack,Store, Trophy,UserRound,LogOut,Menu,X,Shield,Award,TrendingUp} from 'lucide-react';import {useAuth} from '../hooks/useAuth';
const links=[['/dashboard','Dashboard',LayoutDashboard],['/quests','Quests',Swords],['/inventory','Inventory',Backpack],['/shop','Shop',Store],['/leaderboard','Leaderboard',Trophy],['/achievements','Achievements',Award],['/progress','Progress',TrendingUp],['/profile','Profile',UserRound]] as const;
export default function Layout({children}:{children:ReactNode}){const[open,setOpen]=useState(false);const{user,logout}=useAuth();const nav=useNavigate();return <div className="min-h-screen"><aside className={`fixed inset-y-0 left-0 z-50 w-64 border-r border-white/5 bg-[#080d14]/95 p-4 backdrop-blur-xl transition-transform lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="flex items-center justify-between px-2 py-3"><div className="flex items-center gap-2 font-display text-xl"><Shield className="gold" size={23}/>Ascend</div><button onClick={()=>setOpen(false)} className="lg:hidden"><X/></button></div><div className="my-5 rounded-xl border border-amber-400/10 bg-amber-400/[.04] p-3"><div className="text-xs text-slate-500">HERO</div><div className="font-semibold">{user?.name}</div><div className="mt-1 text-xs gold">Level {user?.level} · {user?.gold} Gold</div></div><nav className="space-y-1">{links.map(([to,label,I])=><NavLink onClick={()=>setOpen(false)} key={to} to={to} className={({isActive})=>`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm transition ${isActive?'bg-amber-400/10 text-amber-300':'text-slate-400 hover:bg-white/5 hover:text-white'}`}><I size={18}/>{label}</NavLink>)}</nav><button onClick={()=>{logout();nav('/')}} className="absolute bottom-5 left-4 right-4 flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm text-slate-500 hover:bg-rose-500/10 hover:text-rose-300"><LogOut size={18}/>Logout</button></aside><div className="lg:pl-64"><header className="sticky top-0 z-40 flex h-16 items-center justify-between border-b border-white/5 bg-[#070b10]/80 px-4 backdrop-blur-xl lg:hidden"><button onClick={()=>setOpen(true)}><Menu/></button><div className="font-display">Ascend</div><div className="text-sm gold">{user?.gold}G</div></header><main className="mx-auto max-w-7xl p-4 sm:p-6 lg:p-8">{children}</main></div></div>}
