import {motion} from 'framer-motion';import {Sparkles,Trophy} from 'lucide-react';export default function LevelModal({level,onClose}:{level:number;onClose:()=>void}){return <div className="fixed inset-0 z-[100] grid place-items-center bg-black/75 p-5 backdrop-blur-sm"><motion.div initial={{scale:.8,opacity:0}} animate={{scale:1,opacity:1}} className="glass w-full max-w-md rounded-3xl p-8 text-center shadow-glow"><Sparkles className="mx-auto gold" size={46}/><div className="mt-5 text-sm uppercase tracking-[.3em] gold">Ascension</div><h2 className="mt-2 font-display text-4xl">Level {level}</h2><p className="mt-3 text-slate-400">Your discipline has forged a stronger hero. Keep climbing.</p><Trophy className="mx-auto my-6 text-amber-300" size={42}/><button onClick={onClose} className="btn-gold w-full rounded-xl px-5 py-3">Continue the Journey</button></motion.div></div>}
